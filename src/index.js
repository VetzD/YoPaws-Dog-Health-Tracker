import React from "react";
import ReactDOM from "react-dom/client";
import YoPawsHealthTracker from "./App";
import "./styles.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <YoPawsHealthTracker />
  </React.StrictMode>
);
